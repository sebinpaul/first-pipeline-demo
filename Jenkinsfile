peline {
    agent any

    stages {
        stage('Deploy to Apache') {
            steps {
                // Runs your exact copy command on the Ubuntu server
                sh 'cp index.html /var/www/html/index.html'
            }
        }
    }
}
